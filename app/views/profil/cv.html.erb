<div class="flex">
  <div class="scroll">
      <%= render 'shared/list'%>
  </div>
   <div class="show">
    <div class="container">
      <div class="work">
        <div class="entete"><h1>CV</h1></div>
        <% @cv_categories.each do |cat|%>
        <h2 class="marginbottom bold black"><%=cat.category_title%></h2>
        <div class="marginbottom1 margintop1 paragraph"><%=simple_format(cat.content)%></div>
        <%end%>
      </div>
    </div>
  </div>
</div>
