<div class="flex">
  <div class="scroll">
    <div class="list">
      <%= render 'shared/list'%>
    </div>
  </div>
   <div class="show">
    <div class="container">
      <div class="work">
        <div class="entete"><h1>CV</h1></div>
        <% @cv_categories.each do |cat|%>
        <div class="marginbottom bold black"><%=cat.category_title%></div>
        <div class="marginbottom"><%=simple_format(cat.content)%></div>
        <%end%>
      </div>
    </div>
  </div>
</div>
